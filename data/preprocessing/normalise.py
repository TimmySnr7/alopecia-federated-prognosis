"""Normalisation entry points for image and metadata preprocessing."""

from pathlib import Path


def normalise_dataset(input_dir: Path, output_dir: Path) -> tuple[Path, Path]:
    """Placeholder entry point for dataset normalisation."""
    return input_dir, output_dir
